# AGENTS.md

面向 AI 编码代理的项目约束。

## 当前任务

先阅读：

1. `docs/project/STATUS.zh-CN.md`
2. 状态文档指向的最新实验计划与正式报告
3. 最新的实验分流决策

不要根据过期任务说明重跑已完成实验。当前 CM01-RP 已完成，历史模型码义复用改写分支已终止；详见 `docs/project/CM01-RP_DECISION.zh-CN.md`。

## 每轮结束后的必做决策

- 每轮实验完成并审计后，必须依据结果明确选择“规划下一轮”或“终止当前实验分支”，不能只报告指标或笼统建议继续。
- 若继续，写出下一轮可证伪问题、与本轮证据的联系、输入与候选选择边界、冻结主判据、对照、预算和各结果对应的停止/继续条件；使用新的 run_id。
- 若终止，记录触发停止的证据、终止范围、仍成立的结论及重新开启所需的新证据。不要把单一分支终止写成整个研究问题已被否定。
- 同步更新研究状态及分流决策文件，并在给用户的最终回复中说明决定。
- 规划下一轮不等于自动无限运行；不得为延续实验而调宽旧门槛、改写负结果或重复普通调参。

## 不可破坏的约束

- `results/` 与 `evidence_readonly/` 中已有文件视为只读证据。
- 不用事后指标替换预注册主判据。
- 不读取验证/测试标签选择训练候选。
- 不把 oracle、参数注入或完整任务表搜索描述为自主协议形成。
- 修正实验使用新的 `run_id`，不得覆盖旧结果。
- 保留负结果、失败门槛、配置锁、环境记录和源码哈希。

## 工程约定

- Python 包位于 `src/`，测试位于 `tests/`。
- 本地试验输出写入 `outputs/<experiment>/<run_id>/`。
- 运行测试：`python -m unittest discover -s tests -v`。
- 提交前检查 `git diff --check`，并确认没有缓存、虚拟环境或临时输出进入 Git。
